{
  "name": "froom",
  "version": "0.0.0",
  "author": "Robin Millette <robin@millette.info>",
  "scripts": {
    "dev": "standard && n use 7.10.0 server",
    "build": "standard && next build",
    "now-build": "next build",
    "start": "NODE_ENV=production n use 7.10.0 server",
    "now-start": "NODE_ENV=production node server",
    "lint": "standard",
    "now": "standard && now -E",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engine": {
    "node": ">=7.6"
  },
  "dependencies": {
    "async-lru": "^1.1.1",
    "dotenv-safe": "^4.0.4",
    "isomorphic-fetch": "^2.2.1",
    "koa": "^2.2.0",
    "koa-body": "^2.0.1",
    "koa-router": "^7.2.0",
    "lodash.pick": "^4.4.0",
    "next": "^2.4.0",
    "nprogress": "^0.2.0",
    "pify": "^3.0.0",
    "prop-types": "^15.5.10",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "standard": "^10.0.2"
  }
}
